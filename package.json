{
  "name": "clawos",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/configs.test.mjs",
    "test:dashboard": "cd apps/dashboard && npm run build",
    "validate": "npm test && npm run test:dashboard"
  }
}
